# Отчёты диагностики — содержат чувствительные данные (внутренние IP, hostname, логи).
*_dns_diag_*.txt

# macOS
.DS_Store

# Временное
*.bak

# Локальные инструкции для AI-агентов — не публикуем в репозитории
AGENTS.md
